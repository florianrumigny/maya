interface:
  display_name: "Design Director"
  short_description: "Explore, critique, and systematize a product design direction"
  default_prompt: "Use $design-director to critique the current state, explore distinct directions, and establish a durable design contract."
policy:
  allow_implicit_invocation: true
